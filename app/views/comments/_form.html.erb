<%= bootstrap_form_for ([@message, @message.comments.build]) do |f| %>
  <%= f.text_area :content, label: "Your Comment", class:"bg-dark text-white" %>
  <%= f.submit "Post Comment", class:"btn btn-primary" %>
<% end %>
